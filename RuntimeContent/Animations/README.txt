RUNTIME HUMANOID MOTIONS

Quaternius/AnimationLibrary_Godot_Standard.gltf contains 46 CC0 Humanoid
animation clips. Keep its accompanying .bin file in the same directory.

The application retargets these clips to every valid Humanoid VRM. If the
library is unavailable, the built-in procedural pose system is used instead.
